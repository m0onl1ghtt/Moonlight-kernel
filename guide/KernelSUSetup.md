> [!NOTE]
> This guide is under construction. Please contribute with fork this repo and pull request if you can.

> [!CAUTION]
> Below is all of the script setup KernelSU variants, some require manual hook, some require kprobes hook.

### KernelSU original (v1.0.1 - end of non-gki support)
```
curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s v1.0.1
```

### KernelSU Next (legacy branch)
```
 curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s legacy
```

### RKSU (latest)
```
curl -LSs "https://raw.githubusercontent.com/rsuntk/KernelSU/main/kernel/setup.sh" | bash -s main
```

### MKSU (latest)
```
ugh i can't find it.
```

### WildKSU (latest)
```
curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s wild
```

### CloudSU (latest)
```
curl -LSs "https://raw.githubusercontent.com/KOWX712/KernelSU/kernel/setup.sh" | bash -s master
```

### SukiSU Ultra (meh)
```
find it your self sir
```
